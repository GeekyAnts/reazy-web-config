{
  "name": "reazy-web-config",
  "description": "A simple configuration module for your Reazy web application.",
  "version": "0.0.5",
  "main": "dist/service/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/GeekyAnts/reazy-web-config.git"
  },
  "keywords": [
    "reazy",
    "reazy-web-config",
    "react",
    "native",
    "framework"
  ],
  "author": "Kumar Sanket, Himanshu Satija, Sankhadeep Roy",
  "license": "Apache-2.0",
  "engines": {
    "node": ">= 4"
  },
  "scripts": {
    "build": "rm -rf dist && babel src --presets babel-preset-es2015 --out-dir dist",
    "watch": "rm -rf dist && babel src --presets babel-preset-es2015 --watch --out-dir dist"
  },
  "reazy-setup": "dist/setup/index.js",
  "devDependencies": {
    "babel-cli": "^6.1.4",
    "babel-core": "^6.1.4",
    "chokidar": "^1.6.1",
    "babel-preset-es2015": "^6.1.4"
  },
  "dependencies": {
    "chalk": "^1.1.1",
    "fs-extra": "^2.0.0",
    "reazy-setup-helper": "^0.0.5",
    "yeoman-generator": "^0.24.1"
  }
}
